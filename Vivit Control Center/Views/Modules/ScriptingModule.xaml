<modules:BaseSimpleModule
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:modules="clr-namespace:Vivit_Control_Center.Views.Modules"
    x:Class="Vivit_Control_Center.Views.Modules.ScriptingModule"
    Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top toolbar -->
        <ToolBarTray Grid.Row="0">
            <ToolBar>
                <Button Name="btnRun" Click="btnRun_Click" ToolTip="Ausfuehren (F5 / Ctrl+Enter)" Padding="6,2">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text=">>" Margin="0,0,6,0" />
                        <TextBlock Text="Ausfuehren" />
                    </StackPanel>
                </Button>
                <Separator />
                <Button Name="btnOpen" Click="btnOpen_Click" ToolTip="Oeffnen (Ctrl+O)" Padding="6,2">Oeffnen</Button>
                <Button Name="btnSave" Click="btnSave_Click" ToolTip="Speichern (Ctrl+S)" Padding="6,2">Speichern</Button>
                <Button Name="btnSaveAs" Click="btnSaveAs_Click" ToolTip="Speichern unter" Padding="6,2">Speichern unter</Button>
                <Separator />
                <Button Command="ApplicationCommands.Undo" Padding="6,2">Rueckgaengig</Button>
                <Button Command="ApplicationCommands.Redo" Padding="6,2">Wiederholen</Button>
                <Separator />
                <Button Command="ApplicationCommands.Cut" Padding="6,2">Ausschneiden</Button>
                <Button Command="ApplicationCommands.Copy" Padding="6,2">Kopieren</Button>
                <Button Command="ApplicationCommands.Paste" Padding="6,2">Einfuegen</Button>
                <Separator />
                <Button Name="btnClear" Click="btnClear_Click" Padding="6,2">Leeren</Button>
                <Separator />
                <TextBlock Text="Sprache:" VerticalAlignment="Center" Margin="8,0,4,0" />
                <TextBlock Name="txtDetectedLanguage" FontWeight="Bold" VerticalAlignment="Center" />
            </ToolBar>
        </ToolBarTray>

        <!-- Main editor/output area -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="5" />
                <RowDefinition Height="2*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBox Name="txtScript"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="14"
                         TextChanged="txtScript_TextChanged"
                         KeyDown="txtScript_KeyDown"
                         PreviewKeyDown="txtScript_PreviewKeyDown"
                         Padding="6"/>

                <!-- Simple completion popup -->
                <Popup Name="completionPopup"
                       PlacementTarget="{Binding ElementName=txtScript}"
                       Placement="RelativePoint"
                       StaysOpen="False"
                       AllowsTransparency="True"
                       PopupAnimation="Fade"
                       Width="260">
                    <Border Background="#FF2D2D30" BorderBrush="#FF3F3F46" BorderThickness="1" CornerRadius="4">
                        <ListBox Name="completionList" MouseDoubleClick="completionList_MouseDoubleClick"
                                 Foreground="White" Background="Transparent"
                                 PreviewKeyDown="completionList_PreviewKeyDown"/>
                    </Border>
                </Popup>
            </Grid>

            <GridSplitter Grid.Row="1" Height="5" HorizontalAlignment="Stretch" Background="#FFE5E5E5" />

            <TextBox Grid.Row="2" Name="txtOutput" IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" FontSize="13" Padding="6"
                     TextWrapping="NoWrap"/>
        </Grid>
    </Grid>
</modules:BaseSimpleModule>