<modules:BaseSimpleModule x:Class="Vivit_Control_Center.Views.Modules.SocialModule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:modules="clr-namespace:Vivit_Control_Center.Views.Modules">
    <Grid>
        <Grid.Resources>
            <!-- Dark ComboBox template for this module -->
            <Style x:Key="SocialDarkComboBox" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                <Setter Property="Background" Value="{StaticResource DarkControlBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource DarkForegroundBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource DarkBorderBrush}"/>
                <Setter Property="Padding" Value="6,3,4,3"/>
                <Setter Property="MinHeight" Value="28"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid MinHeight="{TemplateBinding MinHeight}">
                                <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="3"/>
                                <DockPanel>
                                    <ToggleButton x:Name="DropDownToggle"
                                                  DockPanel.Dock="Right"
                                                  IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                  Focusable="False"
                                                  Background="{StaticResource DarkControlBrush}"
                                                  BorderBrush="{StaticResource DarkBorderBrush}"
                                                  Width="20"
                                                  Padding="0">
                                        <Path Data="M 0 0 L 8 0 L 4 4 Z"
                                              Fill="{StaticResource DarkForegroundBrush}"
                                              Stretch="Uniform"
                                              Width="8" Height="6"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </ToggleButton>
                                    <ContentPresenter Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      RecognizesAccessKey="True"
                                                      Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource TemplatedParent}}"
                                                      ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                                      ContentStringFormat="{Binding SelectionBoxItemStringFormat, RelativeSource={RelativeSource TemplatedParent}}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                                </DockPanel>
                                <Popup x:Name="PART_Popup"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       Placement="Bottom"
                                       AllowsTransparency="True"
                                       Focusable="False">
                                    <Border Background="{StaticResource DarkControlBrush}"
                                            BorderBrush="{StaticResource DarkBorderBrush}"
                                            BorderThickness="1">
                                        <ScrollViewer CanContentScroll="True">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="SocialDarkComboBoxItem" TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                <Setter Property="Background" Value="{StaticResource DarkControlBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource DarkForegroundBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource DarkBorderBrush}"/>
                <Setter Property="MinHeight" Value="24"/>
                <Style.Triggers>
                    <Trigger Property="IsHighlighted" Value="True">
                        <Setter Property="Background" Value="{StaticResource DarkControlHoverBrush}"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{StaticResource DarkSelectionBrush}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Margin="8" Grid.Row="0">
            <TextBlock Text="Social Network:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox x:Name="cmbSocial" Width="220" SelectionChanged="cmbSocial_SelectionChanged"
                      Style="{StaticResource SocialDarkComboBox}"
                      ItemContainerStyle="{StaticResource SocialDarkComboBoxItem}"
                      Background="{StaticResource DarkControlBrush}"
                      Foreground="{StaticResource DarkForegroundBrush}"
                      BorderBrush="{StaticResource DarkBorderBrush}">
                <ComboBoxItem Content="Facebook"/>
                <ComboBoxItem Content="X.com"/>
                <ComboBoxItem Content="Bluesky"/>
                <ComboBoxItem Content="Fediverse"/>
            </ComboBox>
        </DockPanel>
        <ContentControl Grid.Row="1" x:Name="BrowserHost" Background="{StaticResource DarkControlBrush}" />
    </Grid>
</modules:BaseSimpleModule>
